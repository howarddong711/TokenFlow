/**
 * Compatibility shim kept only to avoid breaking legacy imports during the
 * TokenFlow migration to the native Tauri + Rust provider runtime.
 *
 * No runtime behavior should depend on this module.
 */

export {};
